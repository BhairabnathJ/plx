import { mutation } from "../_generated/server";

export const group_habits_profile_learning = mutation({
  args: {},
  handler: async () => ({
    ok: true,
    feature: "group_habits_profile_learning",
    note: "Backend contract stub ready for Claude UI integration",
  }),
});
