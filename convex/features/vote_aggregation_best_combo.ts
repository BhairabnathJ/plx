import { mutation } from "../_generated/server";

export const vote_aggregation_best_combo = mutation({
  args: {},
  handler: async () => ({
    ok: true,
    feature: "vote_aggregation_best_combo",
    note: "Backend contract stub ready for Claude UI integration",
  }),
});
