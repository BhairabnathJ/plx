import { mutation } from "../_generated/server";

export const ui_shell_routing_foundation = mutation({
  args: {},
  handler: async () => ({
    ok: true,
    feature: "ui_shell_routing_foundation",
    note: "Backend contract stub ready for Claude UI integration",
  }),
});
